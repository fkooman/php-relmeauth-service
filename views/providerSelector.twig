<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8"/>
        <link href="css/style.css" rel="stylesheet">
        <title>Provider Selector</title>
    </head>
    <body>
        <h1>Provider Selector</h1>
        <p>Please select your provider from the list below to authenticate.</p>

        <table>
        {% for key, value in supportedProviders %}
            <tr>
                <td>
                    <form method="post" action="">
                        <input type="hidden" name="selectedProvider" value="{{ key }}">
                        <input type="submit" value="{{ key }}">
                    </form>
                </td>
                <td><span>to authenticate <tt>{{ value }}</tt></span></td>
            </tr>
        {% endfor %}
        </table>
    </body>
</html>
